import { Button } from "antd";
import styled from "styled-components";

export const ContentWrapper = styled.div`
    margin:24px 16px;
    padding:24px;
    min-height:280px;
    background: ${({theme}) => theme.Colors.background.b6 };

`

export const AntButton= styled(Button)`

`